worker_processes 1;

events { worker_connections 1024; }

http {
    server {
        listen <%= ENV["PORT"] %>;
        server_name localhost;

        location / {
            root /app; # Adjusted from /app/public to /app
            index index.html index.htm;
            try_files $uri $uri/ =404;
        }
    }
}
